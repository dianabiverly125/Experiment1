#include <iostream>
#include <conio.h>
using namespace std;

{
	double mass
	double density
	double volume

	cout << "Mass (in g): " << endl; 
	cin >> mass;
	cout << "Volume (in g/cm^3): " << endl;
	cin >> density;
	cout << "Volume of the object = " << (density * (1/4) * mass) / volume << endl; 

	_getch();
	return 0;

}